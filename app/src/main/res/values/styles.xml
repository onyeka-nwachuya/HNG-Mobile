<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="HNG">
        <item name="android:textColor">@android:color/black</item>
        <item name="android:layout_width">wrap_content</item>
        <item name="android:layout_height">wrap_content</item>
        <item name="android:layout_marginTop">@dimen/dp16</item>
    </style>

    <style name="HNG.EditText">
        <item name="android:layout_marginLeft">@dimen/dp32</item>
        <item name="android:layout_marginStart">@dimen/dp32</item>
        <item name="android:layout_marginRight">@dimen/dp32</item>
        <item name="android:layout_marginEnd">@dimen/dp32</item>
        <item name="android:paddingLeft">@dimen/dp8</item>
        <item name="android:paddingStart">@dimen/dp8</item>
        <item name="android:paddingEnd">@dimen/dp8</item>
        <item name="android:paddingRight">@dimen/dp8</item>
    </style>

    <style name="HNG.Button">
        <item name="android:background">@drawable/button</item>
        <item name="android:textSize">@dimen/sp14</item>
        <item name="android:textColor">@android:color/white</item>
        <item name="android:paddingLeft">@dimen/dp50</item>
        <item name="android:paddingStart">@dimen/dp50</item>
        <item name="android:paddingEnd">@dimen/dp50</item>
        <item name="android:paddingRight">@dimen/dp50</item>
    </style>
</resources>
